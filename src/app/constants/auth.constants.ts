const SIGN_UP_PAGE_NAME = 'Sign up';
const AVATAR_SIZE_LIMIT = 700000;

const FULLNAME_PATTERN = '^\\b[a-zA-Z]+\\b \\b[a-zA-Z]+\\b$';
const PASSWORD_PATTERN = '^[A-Za-z0-9_\\D]+$';

export {
  SIGN_UP_PAGE_NAME,
  AVATAR_SIZE_LIMIT,
  FULLNAME_PATTERN,
  PASSWORD_PATTERN,
};
